A Python 2.7 command-line program.

Asks for a bulbapedia page url that links to a page containing a card set.

TODO: save data to sql table in specified pyodbc connection and create table if it doesn't exist
